{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Python: batch_tune_thresholds",
      "type": "python",
      "request": "launch",
      "program": "${workspaceFolder}/tools/batch_tune_thresholds.py",
      "cwd": "${workspaceFolder}",
      "env": {
        "PYTHONPATH": "${workspaceFolder}"
      },
      "console": "integratedTerminal"
    }
  ]
}
